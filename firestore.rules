rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /channels/{channelId} {
      // Anyone can read (kids need the channel list)
      allow read: true;
      // Only the logged-in parent can add or remove channels
      allow write: if request.auth != null;
    }
  }
}
